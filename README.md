
  # Can Farines

  This is a code bundle for Can Farines. The original project is available at https://www.figma.com/design/iD05EkHL8ilQTMSw49nX5N/Can-Farines.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  